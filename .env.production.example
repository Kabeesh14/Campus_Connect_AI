# ===================================================
# PRODUCTION ENVIRONMENT CONFIGURATION (FRONTEND)
# ===================================================

# Set to absolute API URL if frontend and backend are hosted on separate domains,
# or leave blank / use relative path '/api' for unified single-origin deployment.
VITE_API_URL=/api
